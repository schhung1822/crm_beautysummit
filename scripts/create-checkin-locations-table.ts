import { getDB } from '../src/lib/db'; async function run() { const db = getDB(); await db.query('CREATE TABLE IF NOT EXISTS checkin_locations (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255) NOT NULL, allowed_tiers VARCHAR(255) NOT NULL, image_url TEXT, prerequisite VARCHAR(255), nc_order DECIMAL(10, 2), created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP);'); console.log('Done'); process.exit(0); } run();  
